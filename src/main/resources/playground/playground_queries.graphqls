query GET_POKEMONS {
    allPokemon {
        id
        name
    }
}

query SPECIES_BY_TYPE($type: PokemonType) {
    speciesByType(type: $type) {
        id
        name
        type
        pokemons {
            name
        }
    }
}


